<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissão para usar a câmera -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParcialM">

        <!-- [RF001] Login como tela inicial -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ParcialM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- [RF002] Tela de Cadastro -->
        <activity
            android:name=".ui.RegisterActivity"
            android:theme="@style/Theme.ParcialM" />

        <!-- [RF003] Dashboard de Listas -->
        <activity
            android:name=".ui.ShoppingListActivity"
            android:theme="@style/Theme.ParcialM" />

        <!-- [RF003] Adicionar/Editar Lista -->
        <activity
            android:name=".ui.AddEditListActivity"
            android:theme="@style/Theme.ParcialM" />

        <!-- [RF004] Tela de Itens -->
        <activity
            android:name=".ui.ItemActivity"
            android:theme="@style/Theme.ParcialM" />

        <!-- [RF004] Adicionar/Editar Item -->
        <activity
            android:name=".ui.AddEditItemActivity"
            android:theme="@style/Theme.ParcialM" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ParcialM" />

        <!-- FileProvider para compartilhar arquivos da câmera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>